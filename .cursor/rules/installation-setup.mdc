---
description: 
globs: 
alwaysApply: false
---
# KDE 安裝與設定詳細指南

## 安裝流程

### 系統需求

#### 1. 基本需求
- Linux/macOS/Windows (WSL)
- Docker 已安裝並運行
- Bash shell 環境
- 網路連線

#### 2. 推薦配置
- 至少 4GB RAM
- 至少 10GB 可用磁碟空間
- Docker Desktop 或 Docker Engine

### 安裝步驟

#### 1. 下載 KDE
```bash
# 克隆 KDE 倉庫
git clone https://github.com/your-repo/KDE.git
cd KDE

# 或下載壓縮包
wget https://github.com/your-repo/KDE/archive/main.zip
unzip main.zip
cd KDE-main
```

#### 2. 執行安裝腳本
```bash
# 執行安裝腳本
./install.sh

# 或使用 sudo（如果需要）
sudo ./install.sh
```

#### 3. 驗證安裝
```bash
# 檢查 KDE 是否安裝成功
kde --version
kde --help

# 檢查環境變數
echo $KDE_PATH
echo $KDE_SCRIPTS_PATH
```

### 安裝腳本功能

#### 1. 檔案複製
- 將 KDE 腳本複製到系統目錄
- 設定適當的執行權限
- 建立符號連結

#### 2. 環境變數設定
- 設定 `KDE_PATH` 環境變數
- 設定 `KDE_SCRIPTS_PATH` 環境變數
- 更新 shell 配置檔案

#### 3. 依賴檢查
- 檢查 Docker 是否安裝
- 檢查必要的工具是否可用
- 驗證系統配置

## 解除安裝

### 解除安裝步驟

#### 1. 執行解除安裝腳本
```bash
# 執行解除安裝腳本
./uninstall.sh

# 或使用 sudo（如果需要）
sudo ./uninstall.sh
```

#### 2. 清理環境
```bash
# 手動清理環境變數（如果需要）
# 編輯 ~/.bashrc 或 ~/.zshrc
# 移除 KDE 相關的環境變數
```

### 解除安裝腳本功能

#### 1. 檔案移除
- 移除系統目錄中的 KDE 腳本
- 移除符號連結
- 清理暫存檔案

#### 2. 環境變數清理
- 移除 KDE 相關環境變數
- 更新 shell 配置檔案

#### 3. 資料清理
- 詢問是否保留環境資料
- 清理配置檔案

## 配置檔案

### 主要配置檔案

#### 1. kde.env
```bash
# KDE 主要環境變數配置
KDE_PATH=/path/to/kde
KDE_SCRIPTS_PATH=/path/to/kde/scripts
ENVIROMENTS_PATH=/path/to/kde/environments
CUR_ENV=default
```

#### 2. current.env
```bash
# 當前環境配置
CUR_ENV=my-dev-env
ENV_TYPE=kind
K8S_CONTAINER_NAME=my-dev-control-plane
DOCKER_NETWORK=kde-my-dev
```

#### 3. ngrok.env
```bash
# Ngrok 配置
NGROK_TOKEN=your_ngrok_token_here
```

### 環境配置檔案

#### 1. 環境基本配置 (k8s.env)
```bash
# 環境基本設定
ENV_NAME=my-dev
ENV_TYPE=kind
K8S_CONTAINER_NAME=my-dev-control-plane
DOCKER_NETWORK=kde-my-dev
STORAGE_CLASS=local-path
```

#### 2. 環境本地配置 (.env)
```bash
# 環境本地設定（不提交到版本控制）
LOCAL_IP=192.168.1.100
CUSTOM_DNS=8.8.8.8
```

### 配置檔案位置

#### 1. 全域配置
```
KDE_PATH/
├── kde.env              # 主要環境變數
├── current.env          # 當前環境
├── ngrok.env            # Ngrok 配置
└── environments/        # 環境目錄
```

#### 2. 環境配置
```
environments/<env_name>/
├── k8s.env              # 環境基本配置
├── .env                 # 環境本地配置
├── kubeconfig/          # K8s 配置
├── kind-config.yaml     # Kind 配置
├── k3d-config.yaml      # K3D 配置
└── namespaces/          # 專案目錄
```

## 初始化設定

### 首次使用設定

#### 1. 基本初始化
```bash
# 首次使用 KDE
kde

# 系統會引導進行基本設定
# 1. 選擇預設環境類型
# 2. 設定基本配置
# 3. 建立第一個環境
```

#### 2. 環境變數檢查
```bash
# 檢查環境變數是否正確設定
kde check-env

# 或手動檢查
echo $KDE_PATH
echo $KDE_SCRIPTS_PATH
echo $ENVIROMENTS_PATH
```

#### 3. 依賴檢查
```bash
# 檢查系統依賴
kde check-deps

# 檢查 Docker 狀態
docker version
docker ps
```

### 環境初始化

#### 1. 建立第一個環境
```bash
# 建立開發環境
kde start dev-env kind

# 系統會自動：
# 1. 建立環境目錄結構
# 2. 生成配置檔案
# 3. 啟動 K8s 環境
# 4. 設定為當前環境
```

#### 2. 驗證環境
```bash
# 檢查環境狀態
kde status

# 檢查 K8s 連線
kubectl get nodes

# 檢查環境配置
cat environments/dev-env/k8s.env
```

## 配置最佳實踐

### 1. 環境變數管理

#### 全域配置
```bash
# kde.env - 全域設定
KDE_PATH=/opt/kde
KDE_SCRIPTS_PATH=/opt/kde/scripts
ENVIROMENTS_PATH=/opt/kde/environments
```

#### 環境特定配置
```bash
# 每個環境的 k8s.env
ENV_NAME=dev-env
ENV_TYPE=kind
K8S_CONTAINER_NAME=dev-env-control-plane
DOCKER_NETWORK=kde-dev-env
```

### 2. 目錄結構組織

#### 推薦結構
```
/opt/kde/
├── kde.env
├── current.env
├── ngrok.env
├── scripts/
├── environments/
│   ├── dev-env/
│   ├── test-env/
│   └── prod-env/
└── dockerfiles/
```

### 3. 權限設定

#### 檔案權限
```bash
# 設定適當的檔案權限
chmod 755 /opt/kde/scripts/*.sh
chmod 644 /opt/kde/*.env
chmod 600 /opt/kde/ngrok.env
```

#### 目錄權限
```bash
# 設定目錄權限
chmod 755 /opt/kde
chmod 755 /opt/kde/environments
```

## 故障排除

### 安裝問題

#### 1. 權限問題
```bash
# 檢查檔案權限
ls -la /opt/kde/

# 修正權限
sudo chown -R $USER:$USER /opt/kde/
chmod 755 /opt/kde/scripts/*.sh
```

#### 2. 環境變數問題
```bash
# 檢查環境變數
env | grep KDE

# 重新載入 shell 配置
source ~/.bashrc
# 或
source ~/.zshrc
```

#### 3. 路徑問題
```bash
# 檢查 PATH
echo $PATH

# 檢查符號連結
which kde
ls -la $(which kde)
```

### 配置問題

#### 1. 配置檔案找不到
```bash
# 檢查配置檔案位置
find /opt/kde -name "*.env"

# 檢查配置檔案內容
cat /opt/kde/kde.env
```

#### 2. 環境變數未設定
```bash
# 手動設定環境變數
export KDE_PATH=/opt/kde
export KDE_SCRIPTS_PATH=/opt/kde/scripts
export ENVIROMENTS_PATH=/opt/kde/environments

# 永久設定（加入 shell 配置檔案）
echo 'export KDE_PATH=/opt/kde' >> ~/.bashrc
echo 'export KDE_SCRIPTS_PATH=/opt/kde/scripts' >> ~/.bashrc
echo 'export ENVIROMENTS_PATH=/opt/kde/environments' >> ~/.bashrc
```

### 依賴問題

#### 1. Docker 問題
```bash
# 檢查 Docker 狀態
docker version
docker ps

# 啟動 Docker（如果需要）
sudo systemctl start docker
```

#### 2. 網路問題
```bash
# 檢查網路連線
ping 8.8.8.8

# 檢查 DNS
nslookup google.com
```

## 升級與維護

### 升級流程

#### 1. 備份配置
```bash
# 備份重要配置
cp -r /opt/kde/environments /tmp/kde-backup/
cp /opt/kde/*.env /tmp/kde-backup/
```

#### 2. 下載新版本
```bash
# 下載新版本
git pull origin main
# 或重新下載
wget https://github.com/your-repo/KDE/archive/main.zip
```

#### 3. 重新安裝
```bash
# 執行安裝腳本
./install.sh
```

### 維護任務

#### 1. 定期清理
```bash
# 清理不需要的環境
kde remove old-env

# 清理 Docker 映像
docker image prune

# 清理容器
docker container prune
```

#### 2. 配置備份
```bash
# 備份配置
tar -czf kde-config-backup.tar.gz /opt/kde/environments /opt/kde/*.env

# 恢復配置
tar -xzf kde-config-backup.tar.gz -C /
```

#### 3. 日誌管理
```bash
# 查看 KDE 日誌
tail -f /var/log/kde.log

# 清理舊日誌
find /var/log -name "kde*.log" -mtime +30 -delete
```

## 安全性考量

### 1. 檔案權限
- 設定適當的檔案權限
- 保護敏感配置檔案
- 定期檢查權限設定

### 2. 網路安全
- 使用 HTTPS 連線
- 限制網路存取
- 監控網路活動

### 3. 憑證管理
- 定期更新憑證
- 安全儲存 Token
- 使用強密碼

### 4. 存取控制
- 限制管理員存取
- 記錄操作日誌
- 定期審計存取記錄
