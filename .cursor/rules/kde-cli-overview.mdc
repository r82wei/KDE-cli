---
description: 
globs: 
alwaysApply: false
---
# KDE CLI 工具總覽

KDE 是一個強大的 Kubernetes 環境建置與管理 CLI 工具，提供多種 K8s 環境（kind/k3d/外部）的自動化建置、專案管理、雲端代理等功能。

## 主要功能模組

### 1. 環境管理
- **多種 K8s 環境支援**: kind、k3d、外部 K8s
- **環境生命週期管理**: 建立、啟動、停止、重啟、刪除
- **環境切換**: 支援多環境並可快速切換
- **狀態監控**: 即時查看環境運行狀態

### 2. 專案管理
- **專案/命名空間管理**: 建立、部署、卸載專案
- **Git 整合**: 支援從 Git 倉庫下載專案
- **專案集合**: 批量管理多個相關專案
- **容器環境**: 提供開發和部署容器環境

### 3. 雲端代理與連線
- **Cloudflare Tunnel**: 安全的外部連線
- **Ngrok**: 快速的外部連線
- **Telepresence**: 本地開發環境與 K8s 整合
- **Port Forward**: 本地端口轉發

### 4. 運維工具
- **K9s**: 終端機 K8s 管理工具
- **Dashboard**: Web UI 管理介面
- **Image 載入**: 支援載入 Docker 映像到 K8s 環境

## 核心指令架構

```bash
kde <command> [subcommand] [options] [arguments]
```

### 環境管理指令
- `kde list|ls` - 列出所有 K8s 環境
- `kde start <env_name> [kind|k3d|k8s]` - 建立/啟動 K8s 環境
- `kde create <env_name> [kind|k3d|k8s]` - 建立 K8s 環境
- `kde stop [env_name]` - 停止 K8s 環境
- `kde restart [env_name]` - 重啟 K8s 環境
- `kde status` - 顯示環境狀態
- `kde use [env_name]` - 切換環境
- `kde remove|rm` - 移除環境
- `kde reset` - 重置環境

### 專案管理指令
- `kde project|proj|namespace|ns <command> <project_name>` - 專案管理
- `kde projects|projs <command>` - 專案集合管理

### 運維工具指令
- `kde k9s [-p port]` - 啟動 K9s
- `kde dashboard [-p port] [--insecure]` - 啟動 Dashboard
- `kde expose` - Port Forward
- `kde exec` - 進入容器環境
- `kde load-image <image> [env_name]` - 載入映像

### 雲端代理指令
- `kde ngrok <target>` - Ngrok 連線
- `kde cloudflare-tunnel <domain> <target>` - Cloudflare Tunnel
- `kde telepresence <command> [namespace] [workload]` - Telepresence

## 環境變數與配置

### 主要環境變數
- `KDE_SCRIPTS_PATH`: 腳本路徑
- `KDE_PATH`: KDE 根目錄
- `ENVIROMENTS_PATH`: 環境目錄
- `CUR_ENV`: 當前環境
- `KUBECONFIG`: K8s 配置檔案路徑

### 配置檔案
- `kde.env`: 主要環境變數
- `current.env`: 當前環境設定
- `ngrok.env`: Ngrok 配置
- `environments/<env_name>/k8s.env`: 環境配置
- `environments/<env_name>/.env`: 環境本地配置

## 使用場景

1. **開發環境**: 快速建立本地 K8s 環境進行開發
2. **測試環境**: 建立隔離的測試環境
3. **CI/CD**: 自動化部署和測試
4. **教學演示**: 快速展示 K8s 功能
5. **雲端開發**: 透過代理工具進行遠端開發

## 最佳實踐

1. **環境命名**: 使用有意義的環境名稱
2. **專案組織**: 將相關專案組織在專案集合中
3. **配置管理**: 妥善管理環境變數和配置檔案
4. **資源清理**: 定期清理不需要的環境和資源
5. **安全考量**: 使用 Cloudflare Tunnel 等安全代理工具
